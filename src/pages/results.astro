---
import Layout from '../layouts/Layout.astro';
import resultsData from '../../public/data/results.json';
import { 
  Trophy, Award, TrendingUp, CheckCircle, ArrowUpRight, Sparkles, 
  Calendar, Globe, ArrowLeft, Search, GitCompare, Crown, ExternalLink
} from '@lucide/astro';

const { announcements, passPercentageBySemester, passPercentageByDepartment, yearlyTrends, datasets = [] } = resultsData;
---

<Layout title="Results & Analysis Portal" activeTab="results">

  <!-- ========================================== -->
  <!--   RESULTS SELECTION HUB (Default View)     -->
  <!-- ========================================== -->
  <div id="results-hub-view" class="animated-slide-up">
    <!-- Header -->
    <div style="margin-bottom: 2rem; display: flex; flex-direction: column; gap: 0.5rem;">
      <h1 style="font-size: 2rem; font-family: var(--font-display); tracking: -0.03em;">Results & Analysis Hub</h1>
      <p style="color: var(--text-secondary); max-width: 600px; font-size: 1.05rem;">
        Select an examination batch to search student results, view rankings, and perform side-by-side analysis.
      </p>
    </div>

    <!-- Selection Cards Grid -->
    <div class="results-hub-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin-bottom: 2.5rem;">
      {datasets.map(d => (
        <div class="results-card-select card card-interactive" data-exam={d.id} style="display: flex; flex-direction: column; gap: 1rem; cursor: pointer; text-decoration: none; color: inherit;">
          <div style="display: flex; justify-content: space-between; width: 100%; align-items: center;">
            <div style="background-color: rgba(99, 102, 241, 0.1); color: var(--accent-primary); width: 44px; height: 44px; border-radius: var(--radius-sm); display: flex; align-items: center; justify-content: center;">
              <Award size="22" />
            </div>
            <span class="badge badge-success" style="font-size: 0.7rem; text-transform: uppercase;">{d.status}</span>
          </div>
          
          <div>
            <h3 style="font-size: 1.15rem; font-weight: 700; color: var(--text-primary); margin-bottom: 0.25rem;">{d.title}</h3>
            <p style="font-size: 0.85rem; color: var(--text-secondary);">{d.batch}</p>
          </div>
          
          <div style="display: flex; justify-content: space-between; width: 100%; border-top: 1px solid var(--border-color); padding-top: 0.875rem; margin-top: auto; font-size: 0.8rem; color: var(--text-secondary);">
            <span style="display: flex; align-items: center; gap: 0.25rem;"><Calendar size="14" /> {d.date}</span>
            <span style="display: flex; align-items: center; gap: 0.25rem; font-weight: 700; color: var(--accent-primary);">Open Dashboard →</span>
          </div>
        </div>
      ))}
    </div>

    <!-- Grid for Announcements and Analytics -->
    <div style="display: grid; grid-template-columns: 1fr; gap: 1.5rem;" class="md:grid-cols-3">
      <!-- Left 2 Cols: Result Declarations Table -->
      <div class="md:col-span-2" style="display: flex; flex-direction: column; gap: 2rem;">
        <div>
          <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
            <CheckCircle style="color: var(--accent-success);" /> Declared Results & Alerts
          </h2>
          
          <div class="card" style="padding: 0; overflow-x: auto;">
            <table style="width: 100%; border-collapse: collapse; text-align: left; font-size: 0.9rem;">
              <thead>
                <tr style="border-bottom: 1px solid var(--border-color); background-color: var(--bg-tertiary);">
                  <th style="padding: 1rem; font-weight: 700; color: var(--text-secondary);">Result Announcement</th>
                  <th style="padding: 1rem; font-weight: 700; color: var(--text-secondary);">Date</th>
                  <th style="padding: 1rem; font-weight: 700; color: var(--text-secondary);">Status</th>
                  <th style="padding: 1rem; font-weight: 700; color: var(--text-secondary);">Action</th>
                </tr>
              </thead>
              <tbody>
                {announcements.map(res => {
                  const isDeclared = res.status === 'Declared';
                  return (
                    <tr style="border-bottom: 1px solid var(--border-color); transition: background-color 0.2s;" class="hover:bg-tertiary">
                      <td style="padding: 1rem; font-weight: 600; color: var(--text-primary);">{res.title}</td>
                      <td style="padding: 1rem; color: var(--text-secondary); font-size: 0.85rem;">{res.date}</td>
                      <td style="padding: 1rem;">
                        <span class={`badge ${isDeclared ? 'badge-success' : 'badge-warning'}`} style="font-size: 0.75rem;">
                          {res.status}
                        </span>
                      </td>
                      <td style="padding: 1rem;">
                        {isDeclared ? (
                          <a href={res.link} target="_blank" rel="noopener noreferrer" class="btn btn-outline btn-sm" style="font-size: 0.8rem; gap: 0.25rem; white-space: nowrap; padding: 0.35rem 0.65rem;">
                            View Marks <ArrowUpRight size="14" />
                          </a>
                        ) : (
                          <span style="font-size: 0.8rem; color: var(--text-tertiary);">Pending</span>
                        )}
                      </td>
                    </tr>
                  );
                })}
              </tbody>
            </table>
          </div>
        </div>

        <!-- Department Toppers -->
        <div>
          <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
            <Trophy style="color: var(--accent-warning);" /> Department Toppers (NEP Batch)
          </h2>
          
          <div style="display: grid; grid-template-columns: 1fr; gap: 1rem;" class="md:grid-cols-2">
            {passPercentageByDepartment.map(dept => (
              <div class="card" style="display: flex; flex-direction: column; gap: 0.75rem;">
                <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--border-color); padding-bottom: 0.5rem;">
                  <h3 style="font-size: 1.05rem; font-weight: 700;">{dept.department}</h3>
                  <span class="badge badge-success" style="font-size: 0.75rem;">{dept.passPercentage}% Pass</span>
                </div>
                
                <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                  {dept.toppers.map((topper, idx) => (
                    <div style="display: flex; align-items: center; gap: 0.5rem; font-size: 0.85rem;">
                      <Award size="16" style={`color: ${idx === 0 ? 'var(--accent-warning)' : 'var(--text-tertiary)'};`} />
                      <span style="color: var(--text-primary); font-weight: 500;">{topper}</span>
                    </div>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Right Column: Semester Pass Rates & Updates -->
      <div style="display: flex; flex-direction: column; gap: 2rem;">
        <div>
          <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
            <TrendingUp style="color: var(--accent-primary);" /> Semester Pass Rates
          </h2>
          
          <div class="card" style="display: flex; flex-direction: column; gap: 1.25rem;">
            <p style="font-size: 0.875rem; color: var(--text-secondary); margin-bottom: 0.5rem;">
              Comparative analysis of pass percentages across the undergraduate batches.
            </p>
            
            {passPercentageBySemester.map(item => (
              <div style="display: flex; flex-direction: column; gap: 0.35rem;">
                <div style="display: flex; justify-content: space-between; align-items: center; font-size: 0.85rem;">
                  <span style="font-weight: 700; color: var(--text-primary);">{item.semester}</span>
                  <span style="font-size: 0.8rem; color: var(--text-secondary);">
                    <strong style="color: var(--text-primary);">{item.passPercentage}%</strong> ({item.totalStudents} students)
                  </span>
                </div>
                
                <div style="background-color: var(--bg-tertiary); height: 10px; border-radius: var(--radius-full); overflow: hidden; display: flex;">
                  <div style={`width: ${item.passPercentage}%; background: linear-gradient(90deg, var(--accent-primary) 0%, var(--accent-success) 100%); border-radius: var(--radius-full);`}></div>
                </div>
              </div>
            ))}
          </div>
        </div>

        <!-- Phase 2 Upgrades -->
        <div class="card" style="background: linear-gradient(135deg, rgba(99, 102, 241, 0.05) 0%, rgba(16, 185, 129, 0.05) 100%); border: 1px dashed var(--border-color); padding: 1.25rem;">
          <h3 style="font-size: 0.95rem; font-weight: 700; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.4rem; color: var(--accent-primary);">
            <Sparkles size="16" /> Phase 2 Upgrades
          </h3>
          <p style="font-size: 0.8rem; color: var(--text-secondary); line-height: 1.4;">
            <strong>Interactive Analytics Reminder</strong>: Chart.js dynamic visual representations (Semester-wise bar graphs, yearly linear trends, and comparison templates) will be configured in the next build session.
          </p>
        </div>
      </div>
    </div>
  </div>


  <!-- ========================================== -->
  <!--   EXAM DASHBOARD VIEW (Initially Hidden)   -->
  <!-- ========================================== -->
  <div id="results-dashboard-view" style="display: none; flex-direction: column; gap: 1.5rem;" class="animated-slide-up">
    
    <!-- Back to Hub Button -->
    <button class="btn btn-secondary" id="dashboard-back-btn" style="width: fit-content; align-items: center; gap: 0.5rem; font-size: 0.9rem; font-weight: 600; padding: 0.5rem 1rem;">
      ← Back to Results Hub
    </button>

    <!-- Dashboard Title Section -->
    <div style="display: flex; flex-direction: column; gap: 0.25rem;">
      <h2 id="dashboard-exam-title" style="font-size: 1.8rem; font-family: var(--font-display); tracking: -0.02em;">-</h2>
      <p id="dashboard-exam-subtitle" style="color: var(--text-secondary); font-size: 1rem;">-</p>
    </div>

    <!-- Major Filter select (if multi-major) -->
    <div id="dashboard-filter-container" class="card" style="padding: 1rem; display: none; align-items: center; gap: 1rem;">
      <span style="font-size: 0.9rem; font-weight: 700; color: var(--text-secondary); display: flex; align-items: center; gap: 0.35rem;">
        🔍 Filter Major:
      </span>
      <select id="dashboard-major-select" class="form-input" style="width: auto; height: 38px; padding: 0 1rem; font-size: 0.9rem; border-radius: var(--radius-xs); cursor: pointer;">
        <option value="All">All Majors</option>
      </select>
    </div>

    <!-- Stats Cards Grid -->
    <div class="results-stats-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem;">
      <!-- Total Enrolled -->
      <div class="card" style="display: flex; flex-direction: column; gap: 0.5rem;">
        <div style="display: flex; justify-content: space-between; align-items: center; color: var(--text-secondary); font-size: 0.85rem; font-weight: 600;">
          <span>Total Enrolled</span>
          <span style="font-size: 1.15rem;">👥</span>
        </div>
        <div id="stat-total-students" style="font-size: 1.75rem; font-weight: 800; color: var(--text-primary);">0</div>
        <span style="font-size: 0.75rem; color: var(--text-secondary);">Students in batch</span>
      </div>

      <!-- Pass Percentage -->
      <div class="card" style="display: flex; flex-direction: column; gap: 0.5rem;">
        <div style="display: flex; justify-content: space-between; align-items: center; color: var(--text-secondary); font-size: 0.85rem; font-weight: 600;">
          <span>Pass Percentage</span>
          <span style="color: var(--accent-success); font-size: 1.15rem;">✅</span>
        </div>
        <div id="stat-pass-percent" style="font-size: 1.75rem; font-weight: 800; color: var(--text-primary);">0%</div>
        <span id="stat-pass-sub" style="font-size: 0.75rem; color: var(--text-secondary);">0 of 0 Passed</span>
        <div style="background-color: var(--bg-tertiary); height: 6px; border-radius: var(--radius-full); overflow: hidden; margin-top: 0.25rem;">
          <div id="stat-pass-progress" style="width: 0%; background: var(--accent-success); height: 100%; border-radius: var(--radius-full); transition: width 0.3s;"></div>
        </div>
      </div>

      <!-- Batch Topper -->
      <div class="card" id="stat-topper-card" style="display: flex; flex-direction: column; gap: 0.5rem; cursor: pointer;">
        <div style="display: flex; justify-content: space-between; align-items: center; color: var(--text-secondary); font-size: 0.85rem; font-weight: 600;">
          <span>Batch Topper</span>
          <span style="color: var(--accent-warning); font-size: 1.15rem;">👑</span>
        </div>
        <div id="stat-topper-score" style="font-size: 1.75rem; font-weight: 800; color: var(--text-primary);">0</div>
        <span id="stat-topper-name" style="font-size: 0.75rem; font-weight: 700; color: var(--accent-warning); text-overflow: ellipsis; white-space: nowrap; overflow: hidden; width: 100%; display: block;" title="-">-</span>
      </div>

      <!-- Batch Average -->
      <div class="card" style="display: flex; flex-direction: column; gap: 0.5rem;">
        <div style="display: flex; justify-content: space-between; align-items: center; color: var(--text-secondary); font-size: 0.85rem; font-weight: 600;">
          <span>Batch Average</span>
          <span style="color: var(--accent-primary); font-size: 1.15rem;">📈</span>
        </div>
        <div id="stat-avg-score" style="font-size: 1.75rem; font-weight: 800; color: var(--text-primary);">0</div>
        <span style="font-size: 0.75rem; color: var(--text-secondary);">Avg grand total score</span>
      </div>
    </div>

    <!-- Student Search Section -->
    <div class="card" id="results-search-section" style="padding: 2rem;">
      <h3 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.5rem;">
        🔍 Search Student Result
      </h3>
      <p style="color: var(--text-secondary); font-size: 0.9rem; margin-bottom: 1.5rem;">
        Enter your roll number or student name to view detailed marks cards.
      </p>

      <div style="display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap; position: relative;">
        <!-- Input wrapper -->
        <div style="position: relative; flex: 1; min-width: 240px;">
          <input type="text" id="result-search-input" class="form-input" placeholder="Type Roll Number or Name..." autocomplete="off" style="height: 44px; padding-left: 2.5rem; border-radius: var(--radius-sm);" />
          <span style="position: absolute; left: 1rem; top: 50%; transform: translateY(-50%); color: var(--text-tertiary); font-size: 1.15rem;">🔍</span>
          
          <!-- Autocomplete suggestions dropdown -->
          <div id="results-search-suggestions" class="card" style="display: none; position: absolute; top: 48px; left: 0; right: 0; z-index: 100; max-height: 250px; overflow-y: auto; padding: 0.5rem; box-shadow: var(--shadow-lg);"></div>
        </div>

        <button class="btn btn-primary" id="result-search-btn" style="height: 44px; padding: 0 1.5rem;">
          Search Result
        </button>

        <button class="btn btn-secondary" id="result-browse-all-btn" style="height: 44px; padding: 0 1.5rem;">
          👥 Browse All
        </button>
      </div>

      <!-- Search Student Output Card -->
      <div id="student-result-output" style="margin-top: 1.5rem;"></div>
    </div>

    <!-- Leaderboard and Toppers Row -->
    <div style="display: grid; grid-template-columns: 1fr; gap: 1.5rem;" class="lg:grid-cols-2">
      
      <!-- Batch Leaderboard -->
      <div class="card" style="padding: 1.5rem; display: flex; flex-direction: column; gap: 1rem;">
        <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 0.75rem;">
          <h3 style="font-size: 1.25rem; font-family: var(--font-display); display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0;">
            🏆 Batch Leaderboard
          </h3>
          
          <div style="display: flex; align-items: center; gap: 0.5rem;">
            <span style="font-size: 0.8rem; font-weight: 700; color: var(--text-secondary);">Sort By:</span>
            <select id="leaderboard-sort-select" class="form-input" style="width: auto; height: 32px; padding: 0 0.5rem; font-size: 0.8rem; border-radius: var(--radius-xs); cursor: pointer;">
              <option value="grand_total">Grand Total (Overall)</option>
            </select>
          </div>
        </div>
        <p id="leaderboard-desc" style="color: var(--text-secondary); font-size: 0.825rem; margin-top: -0.5rem;">Top 10 students by Grand Total in this semester exam.</p>

        <div style="overflow-x: auto; border-top: 1px solid var(--border-color); padding-top: 0.5rem;">
          <table style="width: 100%; border-collapse: collapse; text-align: left; font-size: 0.85rem;">
            <thead>
              <tr style="border-bottom: 1px solid var(--border-color);">
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary); width: 60px; text-align: center;">Rank</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary);">Student Name</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary);">Roll Number</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary); text-align: right; width: 100px;">Total</th>
              </tr>
            </thead>
            <tbody id="leaderboard-table-body">
              <tr>
                <td colspan="4" style="text-align: center; padding: 2rem; color: var(--text-secondary);">Loading leaderboard data...</td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Subject Toppers list -->
      <div class="card" style="padding: 1.5rem; display: flex; flex-direction: column; gap: 1rem;">
        <h3 style="font-size: 1.25rem; font-family: var(--font-display); display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0;">
          🥇 Subject Toppers
        </h3>
        <p style="color: var(--text-secondary); font-size: 0.825rem; margin-top: -0.5rem;">Click a row to view the student's complete marks card.</p>
        
        <div id="subject-toppers-list" style="display: flex; flex-direction: column; gap: 0.75rem; border-top: 1px solid var(--border-color); padding-top: 1rem; max-height: 400px; overflow-y: auto;">
          <div style="text-align: center; padding: 2rem; color: var(--text-secondary);">Loading subject toppers...</div>
        </div>
      </div>

    </div>

    <!-- Student Comparison Widget -->
    <div class="card" id="results-compare-section" style="padding: 2rem;">
      <h3 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.5rem;">
        🔄 Compare Students
      </h3>
      <p style="color: var(--text-secondary); font-size: 0.9rem; margin-bottom: 1.5rem;">
        Enter two roll numbers or student names to compare marks side by side.
      </p>

      <div style="display: flex; gap: 0.75rem; align-items: center; flex-wrap: wrap; justify-content: center; position: relative;">
        <!-- Input 1 -->
        <div style="position: relative; flex: 1; min-width: 200px;">
          <input type="text" id="compare-roll-1" class="form-input" placeholder="Roll Number or Name 1" autocomplete="off" style="height: 44px; padding-left: 2.25rem; border-radius: var(--radius-sm);" />
          <span style="position: absolute; left: 0.85rem; top: 50%; transform: translateY(-50%); color: var(--text-tertiary);">👤</span>
          <div id="compare-suggestions-1" class="card" style="display: none; position: absolute; top: 48px; left: 0; right: 0; z-index: 100; max-height: 200px; overflow-y: auto; padding: 0.5rem; box-shadow: var(--shadow-lg);"></div>
        </div>

        <span style="font-weight: 800; font-size: 0.9rem; color: var(--text-tertiary); background-color: var(--bg-tertiary); padding: 0.35rem 0.75rem; border-radius: var(--radius-full);">VS</span>

        <!-- Input 2 -->
        <div style="position: relative; flex: 1; min-width: 200px;">
          <input type="text" id="compare-roll-2" class="form-input" placeholder="Roll Number or Name 2" autocomplete="off" style="height: 44px; padding-left: 2.25rem; border-radius: var(--radius-sm);" />
          <span style="position: absolute; left: 0.85rem; top: 50%; transform: translateY(-50%); color: var(--text-tertiary);">👤</span>
          <div id="compare-suggestions-2" class="card" style="display: none; position: absolute; top: 48px; left: 0; right: 0; z-index: 100; max-height: 200px; overflow-y: auto; padding: 0.5rem; box-shadow: var(--shadow-lg);"></div>
        </div>

        <button class="btn btn-primary" id="compare-btn" style="height: 44px; padding: 0 1.5rem;">
          Compare
        </button>
      </div>

      <!-- Comparison results output -->
      <div id="comparison-result-output" style="margin-top: 1.5rem;"></div>
    </div>

  </div>


  <!-- ========================================== -->
  <!--   UNIVERSAL MODAL (For Browse All List)   -->
  <!-- ========================================== -->
  <div id="browse-all-modal" class="modal-overlay">
    <div class="modal-container" style="max-width: 650px;">
      <div class="modal-close" id="close-browse-modal-btn">
        ✕
      </div>
      <h2 id="browse-modal-title" style="font-size: 1.35rem; margin-bottom: 0.5rem; font-family: var(--font-display); font-weight: 800;">Browse All Student Results</h2>
      <p style="color: var(--text-secondary); font-size: 0.85rem; margin-bottom: 1.25rem;">
        Click on any student row below to load their detailed marks card.
      </p>

      <div style="display: flex; flex-direction: column; gap: 1rem;">
        <div style="position: relative;">
          <input type="text" id="modal-browse-search" class="form-input" placeholder="Search by student name or roll number..." style="height: 40px; padding-left: 2.25rem; border-radius: var(--radius-xs);" />
          <span style="position: absolute; left: 0.85rem; top: 50%; transform: translateY(-50%); color: var(--text-tertiary); font-size: 0.9rem;">🔍</span>
        </div>

        <div style="max-height: 350px; overflow-y: auto; border: 1px solid var(--border-color); border-radius: var(--radius-sm);">
          <table style="width: 100%; border-collapse: collapse; text-align: left; font-size: 0.85rem;">
            <thead style="background-color: var(--bg-tertiary); position: sticky; top: 0; z-index: 10;">
              <tr style="border-bottom: 1px solid var(--border-color);">
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary); width: 60px; text-align: center;">Rank</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary);">Student Name</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary);">Roll Number</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary); text-align: right; width: 80px;">Total</th>
                <th style="padding: 0.75rem 0.5rem; font-weight: 700; color: var(--text-secondary); text-align: center; width: 80px;">Result</th>
              </tr>
            </thead>
            <tbody id="browse-modal-table-body">
              <!-- Rendered via Javascript -->
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

</Layout>

<style is:global>
  /* Results selection cards styling */
  .results-card-select {
    padding: 1.5rem;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  /* Suggestions items */
  .results-suggestion-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.625rem 0.75rem;
    border-radius: var(--radius-xs);
    cursor: pointer;
    transition: background-color 0.15s;
    font-size: 0.85rem;
  }
  .results-suggestion-item:hover {
    background-color: var(--bg-tertiary);
  }
  .results-suggestion-item .suggestion-name {
    font-weight: 600;
    color: var(--text-primary);
  }
  .results-suggestion-item .suggestion-roll {
    font-size: 0.75rem;
    color: var(--text-secondary);
    font-family: monospace;
  }
  
  /* Toppers row styling */
  .results-topper-compact-row {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.65rem 0.75rem;
    border-radius: var(--radius-sm);
    cursor: pointer;
    transition: background-color 0.2s;
    border-bottom: 1px solid var(--border-color);
  }
  .results-topper-compact-row:last-child {
    border-bottom: none;
  }
  .results-topper-compact-row:hover {
    background-color: var(--bg-tertiary);
  }
  .rtc-medal {
    font-size: 1.1rem;
    color: var(--accent-warning);
    display: flex;
    align-items: center;
  }
  .rtc-info {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    flex: 1;
    min-width: 0;
  }
  .rtc-subject {
    font-weight: 700;
    font-size: 0.825rem;
    color: var(--text-primary);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .rtc-name {
    font-size: 0.75rem;
    color: var(--text-secondary);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  
  /* Leaderboard styles */
  .leaderboard-row {
    transition: background-color 0.15s;
    cursor: pointer;
  }
  .leaderboard-row:hover {
    background-color: var(--bg-tertiary);
  }
  .leaderboard-row.rank-gold {
    background: linear-gradient(90deg, rgba(245, 158, 11, 0.08) 0%, transparent 80%);
  }
  .leaderboard-row.rank-silver {
    background: linear-gradient(90deg, rgba(148, 163, 184, 0.08) 0%, transparent 80%);
  }
  .leaderboard-row.rank-bronze {
    background: linear-gradient(90deg, rgba(180, 83, 9, 0.08) 0%, transparent 80%);
  }
  .leaderboard-rank {
    font-size: 1.15rem;
    font-weight: 800;
    display: flex;
    justify-content: center;
  }
  .leaderboard-student-cell {
    display: flex;
    align-items: center;
    gap: 0.65rem;
  }
  .leaderboard-avatar {
    width: 28px;
    height: 28px;
    border-radius: var(--radius-full);
    background: linear-gradient(135deg, var(--accent-primary) 0%, #8b5cf6 100%);
    color: white;
    font-weight: 700;
    font-size: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .leaderboard-name-wrapper {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
    min-width: 0;
  }
  .leaderboard-name {
    font-weight: 600;
    color: var(--text-primary);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .leaderboard-roll-sub {
    font-size: 0.7rem;
    color: var(--text-secondary);
    font-family: monospace;
  }
  .leaderboard-total {
    font-weight: 800;
    color: var(--accent-primary);
    background-color: rgba(99, 102, 241, 0.12);
    padding: 0.2rem 0.6rem;
    border-radius: var(--radius-full);
    font-family: monospace;
  }
  
  /* Detailed Marks Card */
  .results-student-card {
    border-radius: var(--radius-md);
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    overflow: hidden;
    box-shadow: var(--shadow-md);
  }
  .results-student-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem;
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.05) 0%, rgba(16, 185, 129, 0.03) 100%);
    border-bottom: 1px solid var(--border-color);
    flex-wrap: wrap;
  }
  .results-student-avatar {
    width: 44px;
    height: 44px;
    border-radius: var(--radius-full);
    background: linear-gradient(135deg, var(--accent-primary) 0%, #8b5cf6 100%);
    color: white;
    font-weight: 800;
    font-size: 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .results-student-info {
    flex: 1;
    min-width: 0;
  }
  .results-student-name {
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--text-primary);
  }
  .results-student-meta {
    display: flex;
    gap: 1rem;
    margin-top: 0.25rem;
    font-size: 0.8rem;
    color: var(--text-secondary);
  }
  .results-student-status {
    display: flex;
    align-items: center;
    gap: 0.35rem;
    padding: 0.35rem 0.85rem;
    border-radius: var(--radius-full);
    font-weight: 700;
    font-size: 0.8rem;
  }
  .results-student-status.pass {
    background: rgba(16, 185, 129, 0.12);
    color: var(--accent-success);
    border: 1px solid rgba(16, 185, 129, 0.25);
  }
  .results-student-status.promoted {
    background: rgba(245, 158, 11, 0.12);
    color: var(--accent-warning);
    border: 1px solid rgba(245, 158, 11, 0.25);
  }
  
  .results-student-stats {
    display: flex;
    gap: 1rem;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--border-color);
    flex-wrap: wrap;
    background-color: var(--bg-tertiary);
  }
  .results-stat-pill {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0.75rem 1rem;
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    flex: 1;
    min-width: 90px;
    text-align: center;
  }
  .results-stat-label {
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--text-secondary);
    letter-spacing: 0.5px;
  }
  .results-stat-value {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--accent-primary);
    margin-top: 0.25rem;
  }
  
  /* Traditional Marks Table */
  .results-marks-table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.9rem;
  }
  .results-marks-table th {
    padding: 0.75rem 1rem;
    font-weight: 700;
    font-size: 0.75rem;
    text-transform: uppercase;
    color: var(--text-secondary);
    border-bottom: 1px solid var(--border-color);
    text-align: left;
    background-color: var(--bg-tertiary);
  }
  .results-marks-table td {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--border-color);
  }
  .results-marks-table tbody tr {
    transition: background-color 0.15s;
  }
  .results-marks-table tbody tr:hover {
    background-color: var(--bg-tertiary);
  }
  
  /* Comparison Styles */
  .compare-cards-row {
    display: flex;
    gap: 1rem;
    align-items: stretch;
    flex-wrap: wrap;
  }
  .compare-student-card {
    flex: 1;
    min-width: 200px;
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    text-align: center;
    position: relative;
    transition: all 0.2s;
  }
  .compare-student-card.compare-winner-card {
    border-color: rgba(99, 102, 241, 0.35);
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.05) 0%, rgba(139, 92, 246, 0.02) 100%);
    box-shadow: 0 4px 15px rgba(99, 102, 241, 0.08);
  }
  .compare-crown {
    position: absolute;
    top: -0.5rem;
    right: 0.75rem;
    font-size: 1.5rem;
    animation: crownBounce 2s ease-in-out infinite;
  }
  @keyframes crownBounce {
    0%, 100% { transform: translateY(0) rotate(-5deg); }
    50% { transform: translateY(-4px) rotate(5deg); }
  }
  
  .compare-avatar {
    width: 48px;
    height: 48px;
    border-radius: var(--radius-full);
    background: linear-gradient(135deg, var(--accent-primary) 0%, #8b5cf6 100%);
    color: white;
    font-weight: 800;
    font-size: 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 0.75rem auto;
    box-shadow: 0 4px 10px rgba(99, 102, 241, 0.2);
  }
  .compare-roll {
    font-size: 0.75rem;
    color: var(--text-secondary);
    font-family: monospace;
    margin-bottom: 1rem;
  }
  .compare-card-stats {
    display: flex;
    justify-content: center;
    gap: 0.75rem;
  }
  .compare-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 0.5rem;
    background: var(--bg-tertiary);
    border-radius: var(--radius-xs);
    min-width: 50px;
    flex: 1;
  }
  .compare-stat-num {
    font-weight: 800;
    font-size: 0.95rem;
    color: var(--text-primary);
  }
  .compare-stat-label {
    font-size: 0.6rem;
    font-weight: 700;
    text-transform: uppercase;
    color: var(--text-secondary);
  }
  .compare-vs-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  
  .compare-table th, .compare-table td {
    text-align: center;
  }
  .compare-table td.compare-winner {
    color: var(--accent-success) !important;
    font-weight: 800;
  }
  .compare-table td.compare-loser {
    color: var(--text-secondary) !important;
    opacity: 0.7;
  }
  
  .compare-radar-wrapper {
    background-color: var(--bg-secondary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-lg);
    padding: 1.25rem;
  }
  .compare-radar-canvas-container {
    position: relative;
    max-width: 480px;
    height: 320px;
    margin: 0 auto;
  }
  
  /* Responsive Views */
  .desktop-marks-view { display: block; }
  .mobile-marks-view { display: none; }
  
  .results-mobile-marks-list {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1rem;
  }
  .results-mobile-mark-row {
    background-color: var(--bg-tertiary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-sm);
    padding: 0.75rem;
  }
  .res-mob-subject-title {
    font-weight: 700;
    font-size: 0.875rem;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    border-bottom: 1px solid var(--border-color);
    padding-bottom: 0.25rem;
  }
  .res-mob-marks-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.35rem;
  }
  .res-mob-mark-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: var(--bg-secondary);
    padding: 0.35rem 0.25rem;
    border-radius: var(--radius-xs);
    border: 1px solid var(--border-color);
  }
  .res-mob-mark-item.highlight {
    background-color: rgba(99, 102, 241, 0.1);
    border-color: rgba(99, 102, 241, 0.3);
  }
  .res-mob-mark-item .label {
    font-size: 0.55rem;
    text-transform: uppercase;
    color: var(--text-secondary);
    font-weight: 700;
  }
  .res-mob-mark-item .value {
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-top: 0.15rem;
  }
  
  .browse-results-row {
    cursor: pointer;
    transition: background-color 0.15s;
  }
  .browse-results-row:hover {
    background-color: var(--bg-tertiary);
  }
  
  @media (max-width: 768px) {
    .desktop-marks-view { display: none; }
    .mobile-marks-view { display: block; }
    
    .compare-vs-divider {
      margin: 0.5rem 0;
      width: 100%;
    }
  }
</style>

<script>
  import Chart from 'chart.js/auto';

  // Constants
  const baseUrl = '/TataCollegeHub';

  // State
  let currentResultsDataset = [];
  let selectedMajor = 'All';
  let comparisonChart = null;

  // DOM Elements
  const hubView = document.getElementById('results-hub-view');
  const dashboardView = document.getElementById('results-dashboard-view');
  const backBtn = document.getElementById('dashboard-back-btn');
  const examCards = document.querySelectorAll('.results-card-select');

  // Dashboard DOM Elements
  const examTitle = document.getElementById('dashboard-exam-title');
  const examSubtitle = document.getElementById('dashboard-exam-subtitle');
  const filterContainer = document.getElementById('dashboard-filter-container');
  const majorSelect = document.getElementById('dashboard-major-select') as HTMLSelectElement;

  // Stats DOM Elements
  const totalStudentsEl = document.getElementById('stat-total-students');
  const passPercentEl = document.getElementById('stat-pass-percent');
  const passSubEl = document.getElementById('stat-pass-sub');
  const passProgressFill = document.getElementById('stat-pass-progress');
  const topperScoreEl = document.getElementById('stat-topper-score');
  const topperNameEl = document.getElementById('stat-topper-name');
  const avgScoreEl = document.getElementById('stat-avg-score');

  // Search DOM Elements
  const searchInput = document.getElementById('result-search-input') as HTMLInputElement;
  const searchBtn = document.getElementById('result-search-btn');
  const searchSuggestions = document.getElementById('results-search-suggestions');
  const searchOutput = document.getElementById('student-result-output');
  const browseAllBtn = document.getElementById('result-browse-all-btn');

  // Leaderboard DOM Elements
  const leaderboardSortSelect = document.getElementById('leaderboard-sort-select') as HTMLSelectElement;
  const leaderboardTableBody = document.getElementById('leaderboard-table-body');
  const leaderboardDesc = document.getElementById('leaderboard-desc');

  // Toppers DOM Element
  const subjectToppersList = document.getElementById('subject-toppers-list');

  // Comparison DOM Elements
  const compareBtn = document.getElementById('compare-btn');
  const compareRoll1 = document.getElementById('compare-roll-1') as HTMLInputElement;
  const compareRoll2 = document.getElementById('compare-roll-2') as HTMLInputElement;
  const compareSuggestions1 = document.getElementById('compare-suggestions-1');
  const compareSuggestions2 = document.getElementById('compare-suggestions-2');
  const comparisonResultOutput = document.getElementById('comparison-result-output');

  // Modal DOM Elements
  const browseModal = document.getElementById('browse-all-modal');
  const closeBrowseModalBtn = document.getElementById('close-browse-modal-btn');
  const modalBrowseSearch = document.getElementById('modal-browse-search') as HTMLInputElement;
  const browseModalTableBody = document.getElementById('browse-modal-table-body');

  // --- Utility: Clean Subject Names ---
  function cleanSubjectName(name) {
    if (!name) return 'Unknown';
    let cleaned = name;
    
    const majorMatch = name.match(/^MAJOR-([lIVX]+)\s*-\s*(.*)/i);
    if (majorMatch) {
      cleaned = `Major ${majorMatch[1].toUpperCase()}: ${majorMatch[2].trim()}`;
    } else if (name.toLowerCase().startsWith('minor')) {
      const paperMatch = name.match(/Paper Name:?-?\s*(.*)/i);
      if (paperMatch) {
        cleaned = `Minor: ${paperMatch[1].trim()}`;
      } else {
        const parts = name.split('-');
        if (parts.length >= 3) {
          cleaned = `Minor: ${parts.slice(2).join('-').trim()}`;
        } else {
          cleaned = name.replace(/^Minor-[A-Z0-9]+-?/i, 'Minor: ');
        }
      }
    } else if (name.toLowerCase().includes('multi disciplinary course')) {
      cleaned = name.replace(/^Multi Disciplinary Course-II\s*-\s*/i, 'MDC: ').replace(/^Multi Disciplinary Course-II\s*/i, 'MDC: ').replace(/Geography-/, 'Geography');
    } else if (name.toLowerCase().includes('skill enhancement course')) {
      cleaned = name.replace(/^Skill Enhancement Course-II\s*-\s*/i, 'SEC: ').replace(/^Skill Enhancement Course-II-?\s*/i, 'SEC: ').replace(/^Skill Enhancement Course-ll\s*-\s*/i, 'SEC: ').replace(/^Skill Enhancement Course-\s*-\s*/i, 'SEC: ');
    } else if (name.toLowerCase().includes('ability enhancement course')) {
      cleaned = name.replace(/^Ability Enhancement Courses-II\s*-\s*/i, 'AEC: ').replace(/^Ability Enhancement Courses-\s*-\s*/i, 'AEC: ').replace(/^Ability Enhancement Courses-II-?\s*/i, 'AEC: ').replace(/^Ability Enhancement Courses-IV\s*-\s*/i, 'AEC: ').replace(/^Ability Enhancement Courses-IV-?\s*/i, 'AEC: ');
    } else if (name.toLowerCase().includes('value addes course') || name.toLowerCase().includes('value added course')) {
      cleaned = name.replace(/^Value Addes Courses-ll\s*-\s*/i, 'VAC: ').replace(/^Value Addes Courses-\s*-\s*/i, 'VAC: ').replace(/^Value Addes Courses-?\s*/i, 'VAC: ');
    } else {
      cleaned = name
        .replace(/^MAJOR-[VIX]+-/i, '')
        .replace(/^Minor-[IIB]+-/i, '')
        .replace(/^Ability Enhancement Courses-[IVX]+ ?- ?/i, 'AEC: ')
        .replace(/^Value Addes Courses-[lIVX]* ?-? ?/i, 'VAC: ')
        .trim();
    }

    return cleaned
      .replace(/Global Citizenship Education for Sustainable Development/gi, "Global Citizenship")
      .replace(/Communication Skills & Personality development/gi, "Comm. Skills & Personality")
      .replace(/News Writing and Reporting/gi, "News Writing")
      .replace(/Chemistry in Everyday Life/gi, "Chem in Everyday Life")
      .replace(/Electrical Circuits and Network Skills/gi, "Electrical Circuits")
      .replace(/Analytical Chemistry/gi, "Analyt. Chem")
      .replace(/Digital Systems and Applications/gi, "Digital Sys")
      .replace(/Digital Systems/gi, "Digital Sys")
      .replace(/Mathematics/gi, "Math")
      .replace(/Composition/gi, "Comp");
  }

  // --- Utility: Get Student Major ---
  function getStudentMajor(student) {
    if (!student || !student.subjects) return 'Unknown';
    for (const key of ['major_ii', 'major_iii', 'major_vi', 'major_vii', 'major_viii']) {
      const subObj = student.subjects[key];
      if (subObj && subObj.subject) {
        const title = subObj.subject.toLowerCase();
        if (title.includes('mathematics') || title.includes('math')) return 'Mathematics';
        if (title.includes('physics')) return 'Physics';
        if (title.includes('chemistry')) return 'Chemistry';
      }
    }
    for (const key in student.subjects) {
      const subObj = student.subjects[key];
      if (subObj && subObj.subject) {
        const title = subObj.subject.toLowerCase();
        if (title.includes('mathematics') || title.includes('math')) return 'Mathematics';
        if (title.includes('physics')) return 'Physics';
        if (title.includes('chemistry')) return 'Chemistry';
      }
    }
    return 'General';
  }

  // --- Router: Hash and query state handler ---
  const handleRouting = async () => {
    // Check search queries e.g., ?exam=... or hash parameters e.g., #results?exam=... or #exam=...
    const urlParams = new URLSearchParams(window.location.search);
    let examId = urlParams.get('exam');
    
    if (!examId && window.location.hash) {
      const hashStr = window.location.hash.substring(1);
      // parse parameters from hash if hash contains a question mark e.g., #results?exam=sem4...
      const queryIdx = hashStr.indexOf('?');
      if (queryIdx !== -1) {
        const hashParams = new URLSearchParams(hashStr.substring(queryIdx + 1));
        examId = hashParams.get('exam');
      } else {
        // e.g. #exam=sem4_maths_2022-2026
        const hashParams = new URLSearchParams(hashStr);
        examId = hashParams.get('exam');
      }
    }

    if (examId) {
      // Find metadata from announcements/datasets
      try {
        const resReg = await fetch(`${baseUrl}/data/results.json`);
        const regData = await resReg.json();
        const datasets = regData.datasets || [];
        const dataset = datasets.find(d => d.id === examId);
        
        if (dataset) {
          await loadExamDashboard(dataset);
        } else {
          showHubView();
        }
      } catch (err) {
        console.error("Routing error:", err);
        showHubView();
      }
    } else {
      showHubView();
    }
  };

  const showHubView = () => {
    if (comparisonChart) {
      comparisonChart.destroy();
      comparisonChart = null;
    }
    if (hubView) hubView.style.display = 'block';
    if (dashboardView) dashboardView.style.display = 'none';
  };

  // --- Dashboard Loader ---
  const loadExamDashboard = async (dataset) => {
    if (examTitle) examTitle.textContent = dataset.title;
    if (examSubtitle) examSubtitle.textContent = `${dataset.batch} — ${dataset.university} • Declared on ${dataset.date}`;
    
    // Reset inputs & output
    if (searchInput) searchInput.value = '';
    if (searchOutput) searchOutput.innerHTML = '';
    if (compareRoll1) compareRoll1.value = '';
    if (compareRoll2) compareRoll2.value = '';
    if (comparisonResultOutput) comparisonResultOutput.innerHTML = '';
    selectedMajor = 'All';

    // Show loading state in lists
    if (leaderboardTableBody) {
      leaderboardTableBody.innerHTML = `<tr><td colspan="4" style="text-align: center; padding: 2rem; color: var(--text-secondary);">Loading leaderboard data...</td></tr>`;
    }
    if (subjectToppersList) {
      subjectToppersList.innerHTML = `<div style="text-align: center; padding: 2rem; color: var(--text-secondary);">Loading toppers...</div>`;
    }

    // Load detailed JSON data
    try {
      const res = await fetch(`${baseUrl}/data/${dataset.file}`);
      currentResultsDataset = await res.json();
      
      // Classify majors for all students
      currentResultsDataset.forEach(s => {
        s.major = getStudentMajor(s);
      });

      // Bind major filter select
      const distinctMajors = [...new Set(currentResultsDataset.map(s => s.major))].filter(m => m !== 'Unknown');
      if (filterContainer && majorSelect && distinctMajors.length > 1) {
        let optHtml = '<option value="All">All Majors</option>';
        distinctMajors.forEach(m => {
          const count = currentResultsDataset.filter(s => s.major === m).length;
          optHtml += `<option value="${m}">${m} Major (${count})</option>`;
        });
        majorSelect.innerHTML = optHtml;
        filterContainer.style.display = 'flex';
      } else if (filterContainer) {
        filterContainer.style.display = 'none';
      }

      // Update dashboard UI elements
      updateDashboardUI();
      
      if (hubView) hubView.style.display = 'none';
      if (dashboardView) dashboardView.style.display = 'flex';
    } catch (err) {
      console.error("Error loading marks dataset: ", err);
      alert("Failed to load results dashboard. Please check your network connection.");
      showHubView();
    }
  };

  const updateDashboardUI = () => {
    // Filter dataset
    const filteredData = selectedMajor === 'All'
      ? currentResultsDataset
      : currentResultsDataset.filter(s => s.major === selectedMajor);

    if (filteredData.length === 0) return;

    // Calculate core statistics
    const totalCount = filteredData.length;
    const passCount = filteredData.filter(s => s.result.toLowerCase() === 'pass').length;
    const passPercent = ((passCount / totalCount) * 100).toFixed(1);
    
    // Sort to find toppers and average
    const sorted = [...filteredData].sort((a, b) => b.grand_total - a.grand_total);
    const topStudent = sorted[0];
    const avgScore = (filteredData.reduce((sum, s) => sum + s.grand_total, 0) / totalCount).toFixed(1);

    // Apply values to stats cards
    if (totalStudentsEl) totalStudentsEl.textContent = totalCount.toString();
    if (passPercentEl) passPercentEl.textContent = `${passPercent}%`;
    if (passSubEl) passSubEl.textContent = `${passCount} of ${totalCount} Passed`;
    if (passProgressFill) passProgressFill.style.width = `${passPercent}%`;
    if (topperScoreEl) topperScoreEl.textContent = topStudent?.grand_total.toString() || '0';
    if (topperNameEl) {
      topperNameEl.textContent = topStudent?.student_name || 'N/A';
      topperNameEl.title = topStudent?.student_name || 'N/A';
    }
    if (avgScoreEl) avgScoreEl.textContent = avgScore.toString();

    // Topper card quick search trigger
    const topperCard = document.getElementById('stat-topper-card');
    if (topperCard && topStudent) {
      topperCard.onclick = () => {
        if (searchInput) {
          searchInput.value = topStudent.roll_number;
          performSearch();
          const searchSec = document.getElementById('results-search-section');
          if (searchSec) searchSec.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      };
    }

    // Update Leaderboard sorting selectors
    const subjectKeysMap = {};
    for (const student of filteredData) {
      if (!student.subjects) continue;
      for (const [key, subObj] of Object.entries(student.subjects)) {
        if (!subObj || subObj.subject === undefined) continue;
        subjectKeysMap[key] = cleanSubjectName(subObj.subject);
      }
    }

    if (leaderboardSortSelect) {
      let optHtml = '<option value="grand_total">Grand Total (Overall)</option>';
      for (const [key, cleanName] of Object.entries(subjectKeysMap)) {
        optHtml += `<option value="${key}">${cleanName}</option>`;
      }
      leaderboardSortSelect.innerHTML = optHtml;
      leaderboardSortSelect.value = 'grand_total';
    }

    renderLeaderboard('grand_total');
    renderSubjectToppers(filteredData);
  };

  // --- Leaderboard Rendering ---
  const renderLeaderboard = (sortBy = 'grand_total') => {
    if (!leaderboardTableBody) return;

    const filteredData = selectedMajor === 'All'
      ? currentResultsDataset
      : currentResultsDataset.filter(s => s.major === selectedMajor);

    if (filteredData.length === 0) {
      leaderboardTableBody.innerHTML = `<tr><td colspan="4" style="text-align: center; padding: 2rem; color: var(--text-secondary);">No leaderboard entries match filter.</td></tr>`;
      return;
    }

    let sorted = [];
    if (sortBy === 'grand_total') {
      sorted = [...filteredData].sort((a, b) => b.grand_total - a.grand_total);
      if (leaderboardDesc) leaderboardDesc.textContent = "Top 10 students by Grand Total in this semester exam.";
    } else {
      sorted = [...filteredData].sort((a, b) => {
        const t1 = a.subjects[sortBy]?.total ?? 0;
        const t2 = b.subjects[sortBy]?.total ?? 0;
        return t2 - t1;
      });
      const subName = subjectToppersList?.querySelector(`[data-key="${sortBy}"]`) ? cleanSubjectName(filteredData[0]?.subjects[sortBy]?.subject) : 'Subject';
      if (leaderboardDesc) leaderboardDesc.textContent = `Top 10 students sorted by marks in ${subName}.`;
    }

    const top10 = sorted.slice(0, 10);

    leaderboardTableBody.innerHTML = top10.map((s, idx) => {
      const rank = idx + 1;
      const medal = rank === 1 ? '🥇' : rank === 2 ? '🥈' : rank === 3 ? '🥉' : rank;
      const rankClass = rank === 1 ? 'rank-gold' : rank === 2 ? 'rank-silver' : rank === 3 ? 'rank-bronze' : '';
      const score = sortBy === 'grand_total' ? s.grand_total : (s.subjects[sortBy]?.total ?? '-');
      const subLabel = sortBy === 'grand_total' ? '' : `<span style="display: block; font-size: 0.72rem; color: var(--text-secondary); font-weight: 500; margin-top: 2px;">Overall: ${s.grand_total}</span>`;

      return `
        <tr class="leaderboard-row ${rankClass}" data-roll="${s.roll_number}">
          <td style="text-align: center; padding: 0.75rem 0.5rem;">
            <span class="leaderboard-rank">${medal}</span>
          </td>
          <td style="padding: 0.75rem 0.5rem;">
            <div class="leaderboard-student-cell">
              <div class="leaderboard-avatar">${s.student_name.charAt(0)}</div>
              <div class="leaderboard-name-wrapper">
                <span class="leaderboard-name">${s.student_name}</span>
                <span class="leaderboard-roll-sub">${s.roll_number}</span>
              </div>
            </div>
          </td>
          <td style="padding: 0.75rem 0.5rem; font-family: monospace; font-size: 0.85rem; color: var(--text-secondary);">${s.roll_number}</td>
          <td style="padding: 0.75rem 0.5rem; text-align: right;">
            <span class="leaderboard-total">${score}</span>
            ${subLabel}
          </td>
        </tr>
      `;
    }).join('');

    // Bind row click
    leaderboardTableBody.querySelectorAll('.leaderboard-row').forEach(row => {
      row.addEventListener('click', () => {
        const roll = row.getAttribute('data-roll');
        if (searchInput) {
          searchInput.value = roll;
          performSearch();
          const searchSec = document.getElementById('results-search-section');
          if (searchSec) searchSec.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });
  };

  // --- Subject Toppers Rendering ---
  const renderSubjectToppers = (filteredData) => {
    if (!subjectToppersList) return;

    const toppersMap = {};
    for (const student of filteredData) {
      if (!student.subjects) continue;
      for (const [key, subObj] of Object.entries(student.subjects)) {
        if (!subObj || subObj.total === null || subObj.total === undefined) continue;
        const score = subObj.total;
        const cleanName = cleanSubjectName(subObj.subject);
        
        if (!toppersMap[key] || score > toppersMap[key].maxScore) {
          toppersMap[key] = {
            student,
            maxScore: score,
            subjectName: cleanName
          };
        }
      }
    }

    const toppersList = Object.entries(toppersMap);

    if (toppersList.length === 0) {
      subjectToppersList.innerHTML = `<div style="text-align: center; padding: 2rem; color: var(--text-secondary);">No subject toppers available.</div>`;
      return;
    }

    subjectToppersList.innerHTML = toppersList.map(([key, item]) => `
      <div class="results-topper-compact-row" data-roll="${item.student.roll_number}" data-key="${key}">
        <div class="rtc-medal">🏅</div>
        <div class="rtc-info">
          <span class="rtc-subject">${item.subjectName}</span>
          <span class="rtc-name">${item.student.student_name}</span>
        </div>
        <span class="rtc-score" style="display: inline-block; flex-shrink: 0; min-width: 38px; text-align: center; white-space: nowrap; padding: 0.2rem 0.6rem; border-radius: var(--radius-full); background: rgba(99, 102, 241, 0.12); border: 1px solid rgba(99, 102, 241, 0.3); color: var(--accent-primary); font-weight: 800; font-size: 0.85rem; font-family: monospace;">${item.maxScore}</span>
      </div>
    `).join('');

    // Bind toppers click
    subjectToppersList.querySelectorAll('.results-topper-compact-row').forEach(row => {
      row.addEventListener('click', () => {
        const roll = row.getAttribute('data-roll');
        if (searchInput) {
          searchInput.value = roll;
          performSearch();
          const searchSec = document.getElementById('results-search-section');
          if (searchSec) searchSec.scrollIntoView({ behavior: 'smooth', block: 'start' });
        }
      });
    });
  };

  // --- Search Results ---
  const performSearch = () => {
    if (!searchInput || !searchOutput) return;
    const query = searchInput.value.toLowerCase().trim();

    if (!query) {
      alert("Please enter a roll number or name to search.");
      return;
    }

    // Resolve query
    const student = currentResultsDataset.find(s => 
      s.roll_number === query || 
      s.student_name.toLowerCase() === query ||
      s.student_name.toLowerCase().includes(query)
    );

    if (!student) {
      searchOutput.innerHTML = `
        <div class="results-error-card animated-slide-up">
          <div class="results-error-icon">⚠️</div>
          <h4>Student Not Found</h4>
          <p>No student matching <strong>"${query}"</strong> was found. Please check the spelling or roll number.</p>
        </div>
      `;
      return;
    }

    renderStudentCard(searchOutput, student);
  };

  const renderStudentCard = (container, student) => {
    const sorted = [...currentResultsDataset].sort((a, b) => b.grand_total - a.grand_total);
    const rank = sorted.findIndex(s => s.roll_number === student.roll_number) + 1;
    const isPass = student.result.toLowerCase() === 'pass';
    const statusClass = isPass ? 'pass' : 'promoted';

    let desktopRows = '';
    let mobileRows = '';

    for (const [key, sub] of Object.entries(student.subjects)) {
      const theory = sub.theory !== null && sub.theory !== undefined ? sub.theory : '-';
      const internal = sub.internal !== null && sub.internal !== undefined ? sub.internal : '-';
      const practical = sub.practical !== null && sub.practical !== undefined ? sub.practical : '-';
      const total = sub.total !== null && sub.total !== undefined ? sub.total : '-';
      const subName = cleanSubjectName(sub.subject);

      desktopRows += `
        <tr class="hover:bg-tertiary">
          <td class="subject-name-cell" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); font-weight: 600;">${subName}</td>
          <td class="mark-cell" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); text-align: center; font-family: monospace;">${theory}</td>
          <td class="mark-cell" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); text-align: center; font-family: monospace;">${internal}</td>
          <td class="mark-cell" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); text-align: center; font-family: monospace;">${practical}</td>
          <td class="mark-cell mark-total" style="padding: 0.75rem 1rem; border-bottom: 1px solid var(--border-color); text-align: center; font-weight: 700; font-family: monospace;">${total}</td>
        </tr>
      `;

      mobileRows += `
        <div class="results-mobile-mark-row">
          <div class="res-mob-subject-title">${subName}</div>
          <div class="res-mob-marks-grid">
            <div class="res-mob-mark-item">
              <span class="label">Theory</span>
              <span class="value">${theory}</span>
            </div>
            <div class="res-mob-mark-item">
              <span class="label">Internal</span>
              <span class="value">${internal}</span>
            </div>
            <div class="res-mob-mark-item">
              <span class="label">Practical</span>
              <span class="value">${practical}</span>
            </div>
            <div class="res-mob-mark-item highlight">
              <span class="label">Total</span>
              <span class="value">${total}</span>
            </div>
          </div>
        </div>
      `;
    }

    container.innerHTML = `
      <div class="results-student-card animated-slide-up">
        <div class="results-student-header">
          <div class="results-student-avatar">${student.student_name.charAt(0)}</div>
          <div class="results-student-info">
            <h3 class="results-student-name">${student.student_name}</h3>
            <div class="results-student-meta">
              <span>🆔 Roll: ${student.roll_number}</span>
              <span>📈 Rank: #${rank} of ${currentResultsDataset.length}</span>
            </div>
          </div>
          <div class="results-student-status ${statusClass}">
            <span>${isPass ? '✓' : '⚠'} ${student.result}</span>
          </div>
        </div>

        <div class="results-student-stats">
          <div class="results-stat-pill">
            <span class="results-stat-label">Grand Total</span>
            <span class="results-stat-value" style="font-family: monospace;">${student.grand_total}</span>
          </div>
          <div class="results-stat-pill">
            <span class="results-stat-label">Subjects</span>
            <span class="results-stat-value">${Object.keys(student.subjects).length}</span>
          </div>
          <div class="results-stat-pill">
            <span class="results-stat-label">Batch Rank</span>
            <span class="results-stat-value">#${rank}</span>
          </div>
        </div>

        <!-- Desktop View Table -->
        <div class="desktop-marks-view" style="overflow-x: auto;">
          <table class="results-marks-table">
            <thead>
              <tr>
                <th>Subject</th>
                <th style="text-align: center;">Theory</th>
                <th style="text-align: center;">Internal</th>
                <th style="text-align: center;">Practical</th>
                <th style="text-align: center;">Total</th>
              </tr>
            </thead>
            <tbody>
              ${desktopRows}
            </tbody>
            <tfoot>
              <tr style="border-top: 2px solid var(--border-color); background-color: var(--bg-tertiary);">
                <td style="padding: 0.75rem 1rem; font-weight: 800; color: var(--text-primary);">Grand Total</td>
                <td></td>
                <td></td>
                <td></td>
                <td style="padding: 0.75rem 1rem; text-align: center; font-weight: 800; color: var(--accent-primary); font-family: monospace; font-size: 1.15rem;">${student.grand_total}</td>
              </tr>
            </tfoot>
          </table>
        </div>

        <!-- Mobile View Stacked Cards -->
        <div class="mobile-marks-view results-mobile-marks-list">
          ${mobileRows}
          <div class="results-mobile-mark-row" style="background: rgba(99, 102, 241, 0.05); border-color: rgba(99, 102, 241, 0.25);">
            <div style="display: flex; justify-content: space-between; align-items: center; font-weight: 800;">
              <span style="font-size: 0.95rem;">Grand Total</span>
              <span style="font-size: 1.2rem; color: var(--accent-primary); font-family: monospace;">${student.grand_total}</span>
            </div>
          </div>
        </div>
      </div>
    `;
  };

  // --- Compare Logic ---
  const performComparison = () => {
    if (!compareRoll1 || !compareRoll2 || !comparisonResultOutput) return;
    const r1 = compareRoll1.value.trim().toLowerCase();
    const r2 = compareRoll2.value.trim().toLowerCase();

    if (!r1 || !r2) {
      alert("Please enter both roll numbers or names to compare.");
      return;
    }

    if (r1 === r2) {
      alert("Please enter two different students to compare.");
      return;
    }

    const s1 = currentResultsDataset.find(s => s.roll_number === r1 || s.student_name.toLowerCase().includes(r1));
    const s2 = currentResultsDataset.find(s => s.roll_number === r2 || s.student_name.toLowerCase().includes(r2));

    const errors = [];
    if (!s1) errors.push(r1);
    if (!s2) errors.push(r2);

    if (errors.length > 0) {
      comparisonResultOutput.innerHTML = `
        <div class="results-error-card animated-slide-up">
          <div class="results-error-icon">⚠️</div>
          <h4>Student Not Found</h4>
          <p>No student matching <strong>"${errors.join('" or "')}"</strong> was found. Please check spelling or roll number.</p>
        </div>
      `;
      return;
    }

    renderComparison(s1, s2);
  };

  const renderComparison = (s1, s2) => {
    if (!comparisonResultOutput) return;

    const sorted = [...currentResultsDataset].sort((a, b) => b.grand_total - a.grand_total);
    const rank1 = sorted.findIndex(s => s.roll_number === s1.roll_number) + 1;
    const rank2 = sorted.findIndex(s => s.roll_number === s2.roll_number) + 1;

    const s1Wins = s1.grand_total > s2.grand_total;
    const tie = s1.grand_total === s2.grand_total;

    const keys = Object.keys(s1.subjects);
    let rows = '';

    for (const key of keys) {
      const sub1 = s1.subjects[key];
      const sub2 = s2.subjects[key];
      if (!sub1 || !sub2) continue;

      const t1 = sub1.total ?? 0;
      const t2 = sub2.total ?? 0;
      const class1 = t1 > t2 ? 'compare-winner' : t1 < t2 ? 'compare-loser' : '';
      const class2 = t2 > t1 ? 'compare-winner' : t2 < t1 ? 'compare-loser' : '';
      const displaySubName = cleanSubjectName(sub1.subject);

      rows += `
        <tr style="border-bottom: 1px solid var(--border-color);">
          <td class="mark-cell ${class1}" style="padding: 0.75rem; font-family: monospace;">${t1}</td>
          <td class="subject-name-cell compare-subject-center" style="padding: 0.75rem; font-weight: 600;">${displaySubName}</td>
          <td class="mark-cell ${class2}" style="padding: 0.75rem; font-family: monospace;">${t2}</td>
        </tr>
      `;
    }

    comparisonResultOutput.innerHTML = `
      <div class="results-compare-output animated-slide-up" style="display: flex; flex-direction: column; gap: 1.5rem;">
        <div class="compare-cards-row">
          <!-- Card 1 -->
          <div class="compare-student-card ${s1Wins && !tie ? 'compare-winner-card' : ''}">
            ${s1Wins && !tie ? '<div class="compare-crown">👑</div>' : ''}
            <div class="compare-avatar">${s1.student_name.charAt(0)}</div>
            <h4>${s1.student_name}</h4>
            <p class="compare-roll">${s1.roll_number}</p>
            <div class="compare-card-stats">
              <div class="compare-stat">
                <span class="compare-stat-num" style="font-family: monospace;">${s1.grand_total}</span>
                <span class="compare-stat-label">Total</span>
              </div>
              <div class="compare-stat">
                <span class="compare-stat-num">#${rank1}</span>
                <span class="compare-stat-label">Rank</span>
              </div>
              <div class="compare-stat">
                <span class="compare-stat-num result-status-${s1.result.toLowerCase()}">${s1.result}</span>
                <span class="compare-stat-label">Status</span>
              </div>
            </div>
          </div>

          <div class="compare-vs-divider">
            <span style="font-weight: 800; color: var(--text-tertiary); background-color: var(--bg-tertiary); padding: 0.35rem 0.75rem; border-radius: var(--radius-full);">VS</span>
          </div>

          <!-- Card 2 -->
          <div class="compare-student-card ${!s1Wins && !tie ? 'compare-winner-card' : ''}">
            ${!s1Wins && !tie ? '<div class="compare-crown">👑</div>' : ''}
            <div class="compare-avatar">${s2.student_name.charAt(0)}</div>
            <h4>${s2.student_name}</h4>
            <p class="compare-roll">${s2.roll_number}</p>
            <div class="compare-card-stats">
              <div class="compare-stat">
                <span class="compare-stat-num" style="font-family: monospace;">${s2.grand_total}</span>
                <span class="compare-stat-label">Total</span>
              </div>
              <div class="compare-stat">
                <span class="compare-stat-num">#${rank2}</span>
                <span class="compare-stat-label">Rank</span>
              </div>
              <div class="compare-stat">
                <span class="compare-stat-num result-status-${s2.result.toLowerCase()}">${s2.result}</span>
                <span class="compare-stat-label">Status</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Comparison Table -->
        <div class="card" style="padding: 0; overflow-x: auto;">
          <table class="results-marks-table compare-table" style="min-width: 100%;">
            <thead>
              <tr style="background-color: var(--bg-tertiary); border-bottom: 1px solid var(--border-color);">
                <th style="padding: 0.75rem; text-align: center;">${s1.student_name.split(' ')[0]}</th>
                <th style="padding: 0.75rem; text-align: center;">Subject</th>
                <th style="padding: 0.75rem; text-align: center;">${s2.student_name.split(' ')[0]}</th>
              </tr>
            </thead>
            <tbody>
              ${rows}
            </tbody>
            <tfoot>
              <tr style="background-color: var(--bg-tertiary); border-top: 2px solid var(--border-color);">
                <td class="mark-cell mark-grand ${s1Wins && !tie ? 'compare-winner' : ''}" style="padding: 0.75rem; font-family: monospace; font-size: 1.1rem; text-align: center;">${s1.grand_total}</td>
                <td class="subject-name-cell compare-subject-center" style="padding: 0.75rem; font-weight: 800; text-align: center;">Grand Total</td>
                <td class="mark-cell mark-grand ${!s1Wins && !tie ? 'compare-winner' : ''}" style="padding: 0.75rem; font-family: monospace; font-size: 1.1rem; text-align: center;">${s2.grand_total}</td>
              </tr>
            </tfoot>
          </table>
        </div>

        <!-- Radar Chart Card -->
        <div class="compare-radar-wrapper">
          <h4 style="font-size: 1rem; font-weight: 700; margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem; color: var(--text-primary);">
            📊 Subject-wise Radar Comparison
          </h4>
          <div class="compare-radar-canvas-container">
            <canvas id="compare-radar-chart"></canvas>
          </div>
        </div>
      </div>
    `;

    renderRadarChart(s1, s2, keys);
  };

  const renderRadarChart = (s1, s2, keys) => {
    const canvas = document.getElementById('compare-radar-chart') as HTMLCanvasElement;
    if (!canvas) return;

    if (comparisonChart) {
      comparisonChart.destroy();
    }

    const labels = keys.map(key => cleanSubjectName(s1.subjects[key]?.subject || s2.subjects[key]?.subject || key));
    const data1 = keys.map(key => s1.subjects[key]?.total ?? 0);
    const data2 = keys.map(key => s2.subjects[key]?.total ?? 0);

    const isDark = document.documentElement.getAttribute('data-theme') === 'dark';
    const gridColor = isDark ? 'rgba(255, 255, 255, 0.08)' : 'rgba(0, 0, 0, 0.08)';
    const labelColor = isDark ? '#94a3b8' : '#475569';

    comparisonChart = new Chart(canvas, {
      type: 'radar',
      data: {
        labels,
        datasets: [
          {
            label: s1.student_name,
            data: data1,
            borderColor: '#6366f1',
            backgroundColor: 'rgba(99, 102, 241, 0.15)',
            borderWidth: 2,
            pointBackgroundColor: '#6366f1',
            pointRadius: 4,
          },
          {
            label: s2.student_name,
            data: data2,
            borderColor: '#f59e0b',
            backgroundColor: 'rgba(245, 158, 11, 0.15)',
            borderWidth: 2,
            pointBackgroundColor: '#f59e0b',
            pointRadius: 4,
          }
        ]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        scales: {
          r: {
            beginAtZero: true,
            grid: { color: gridColor },
            angleLines: { color: gridColor },
            ticks: { color: labelColor, backdropColor: 'transparent', font: { size: 9 } },
            pointLabels: { color: labelColor, font: { size: 9, weight: '600' } }
          }
        },
        plugins: {
          legend: {
            position: 'bottom',
            labels: { color: labelColor, font: { size: 11, weight: '600' }, usePointStyle: true, pointStyle: 'circle' }
          }
        }
      }
    });
  };

  // --- Browse All Modal ---
  const openBrowseAllModal = () => {
    if (!browseModal || !browseModalTableBody) return;

    // Filter and rank data
    const filteredData = selectedMajor === 'All'
      ? currentResultsDataset
      : currentResultsDataset.filter(s => s.major === selectedMajor);

    const sorted = [...filteredData].sort((a, b) => b.grand_total - a.grand_total);

    const renderRows = (list) => {
      return list.map(s => {
        const rank = sorted.findIndex(item => item.roll_number === s.roll_number) + 1;
        const medal = rank === 1 ? '🥇' : rank === 2 ? '🥈' : rank === 3 ? '🥉' : `#${rank}`;
        const isPass = s.result.toLowerCase() === 'pass';

        return `
          <tr class="browse-results-row" data-roll="${s.roll_number}" style="border-bottom: 1px solid var(--border-color);">
            <td style="padding: 0.75rem 0.5rem; text-align: center; font-weight: 800;">${medal}</td>
            <td style="padding: 0.75rem 0.5rem; font-weight: 600; color: var(--text-primary);">
              ${s.student_name}
              <span style="display: block; font-size: 0.7rem; color: var(--text-secondary); font-weight: 500; margin-top: 2px;">Major: ${s.major}</span>
            </td>
            <td style="padding: 0.75rem 0.5rem; font-family: monospace; font-size: 0.8rem; color: var(--text-secondary);">${s.roll_number}</td>
            <td style="padding: 0.75rem 0.5rem; text-align: right; font-weight: 700; color: var(--accent-primary); font-family: monospace;">${s.grand_total}</td>
            <td style="padding: 0.75rem 0.5rem; text-align: center;">
              <span class="badge ${isPass ? 'badge-success' : 'badge-warning'}" style="font-size: 0.7rem;">${s.result}</span>
            </td>
          </tr>
        `;
      }).join('');
    };

    browseModalTableBody.innerHTML = renderRows(sorted);
    browseModal.classList.add('open');

    // Bind click events on rows
    const bindClicks = () => {
      browseModalTableBody.querySelectorAll('.browse-results-row').forEach(row => {
        row.addEventListener('click', () => {
          const roll = row.getAttribute('data-roll');
          if (searchInput) {
            searchInput.value = roll;
            browseModal.classList.remove('open');
            performSearch();
            const searchSec = document.getElementById('results-search-section');
            if (searchSec) searchSec.scrollIntoView({ behavior: 'smooth', block: 'start' });
          }
        });
      });
    };
    bindClicks();

    // Bind modal search filter
    if (modalBrowseSearch) {
      modalBrowseSearch.value = '';
      modalBrowseSearch.oninput = (e) => {
        const query = (e.target as HTMLInputElement).value.toLowerCase().trim();
        const filtered = sorted.filter(s => 
          s.student_name.toLowerCase().includes(query) || 
          s.roll_number.includes(query)
        );

        if (filtered.length === 0) {
          browseModalTableBody.innerHTML = `<tr><td colspan="5" style="text-align: center; padding: 2rem; color: var(--text-secondary);">No matching student results found.</td></tr>`;
        } else {
          browseModalTableBody.innerHTML = renderRows(filtered);
          bindClicks();
        }
      };
    }
  };

  // --- Auto-complete suggestions ---
  const handleSearchAutocomplete = (e) => {
    const val = e.target.value.toLowerCase().trim();
    if (!searchSuggestions) return;

    if (val.length < 1) {
      searchSuggestions.style.display = 'none';
      return;
    }

    const matches = currentResultsDataset.filter(s => {
      const matchesMajor = selectedMajor === 'All' || s.major === selectedMajor;
      const matchesQuery = s.roll_number.includes(val) || s.student_name.toLowerCase().includes(val);
      return matchesMajor && matchesQuery;
    }).slice(0, 6);

    if (matches.length === 0) {
      searchSuggestions.style.display = 'none';
      return;
    }

    searchSuggestions.innerHTML = matches.map(s => `
      <div class="results-suggestion-item" data-roll="${s.roll_number}">
        <span class="suggestion-name">${s.student_name}</span>
        <span class="suggestion-roll">${s.roll_number}</span>
      </div>
    `).join('');

    searchSuggestions.style.display = 'block';

    searchSuggestions.querySelectorAll('.results-suggestion-item').forEach(item => {
      item.addEventListener('click', () => {
        const roll = item.getAttribute('data-roll');
        if (searchInput) searchInput.value = roll;
        searchSuggestions.style.display = 'none';
        performSearch();
      });
    });
  };

  // --- Compare Auto-complete ---
  const handleCompareAutocomplete = (inputEl, suggestionsEl) => {
    if (!inputEl || !suggestionsEl) return;

    inputEl.addEventListener('input', (e) => {
      const val = (e.target as HTMLInputElement).value.toLowerCase().trim();

      if (val.length < 1) {
        suggestionsEl.style.display = 'none';
        return;
      }

      const matches = currentResultsDataset.filter(s => {
        const matchesMajor = selectedMajor === 'All' || s.major === selectedMajor;
        const matchesQuery = s.roll_number.includes(val) || s.student_name.toLowerCase().includes(val);
        return matchesMajor && matchesQuery;
      }).slice(0, 6);

      if (matches.length === 0) {
        suggestionsEl.style.display = 'none';
        return;
      }

      suggestionsEl.innerHTML = matches.map(s => `
        <div class="results-suggestion-item" data-roll="${s.roll_number}">
          <span class="suggestion-name">${s.student_name}</span>
          <span class="suggestion-roll">${s.roll_number}</span>
        </div>
      `).join('');

      suggestionsEl.style.display = 'block';

      suggestionsEl.querySelectorAll('.results-suggestion-item').forEach(item => {
        item.addEventListener('click', () => {
          inputEl.value = item.getAttribute('data-roll');
          suggestionsEl.style.display = 'none';
        });
      });
    });
  };

  // --- Initialization & Event Listeners ---
  const init = () => {
    // 1. Exam cards open dashboard click handlers
    examCards.forEach(card => {
      card.addEventListener('click', () => {
        const examId = card.getAttribute('data-exam');
        window.location.hash = `#results?exam=${examId}`;
      });
    });

    // 2. Back to Selection Hub button click
    backBtn?.addEventListener('click', () => {
      window.location.hash = '#results';
    });

    // 3. Search triggers
    searchBtn?.addEventListener('click', performSearch);
    searchInput?.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') {
        if (searchSuggestions) searchSuggestions.style.display = 'none';
        performSearch();
      }
    });
    searchInput?.addEventListener('input', handleSearchAutocomplete);

    // 4. Modal Triggers
    browseAllBtn?.addEventListener('click', openBrowseAllModal);
    closeBrowseModalBtn?.addEventListener('click', () => {
      browseModal?.classList.remove('open');
    });
    browseModal?.addEventListener('click', (e) => {
      if (e.target === browseModal) browseModal.classList.remove('open');
    });

    // 5. Compare triggers
    compareBtn?.addEventListener('click', performComparison);
    handleCompareAutocomplete(compareRoll1, compareSuggestions1);
    handleCompareAutocomplete(compareRoll2, compareSuggestions2);

    // Dismiss autocompletes when clicking outside
    document.addEventListener('click', (e) => {
      const target = e.target as HTMLElement;
      if (searchInput && searchSuggestions && !searchInput.contains(target) && !searchSuggestions.contains(target)) {
        searchSuggestions.style.display = 'none';
      }
      if (compareRoll1 && compareSuggestions1 && !compareRoll1.contains(target) && !compareSuggestions1.contains(target)) {
        compareSuggestions1.style.display = 'none';
      }
      if (compareRoll2 && compareSuggestions2 && !compareRoll2.contains(target) && !compareSuggestions2.contains(target)) {
        compareSuggestions2.style.display = 'none';
      }
    });

    // 6. Major Filter select trigger
    majorSelect?.addEventListener('change', (e) => {
      selectedMajor = (e.target as HTMLSelectElement).value;
      updateDashboardUI();
      if (searchInput && searchInput.value) performSearch();
      if (compareRoll1 && compareRoll1.value && compareRoll2 && compareRoll2.value) performComparison();
    });

    // 7. Leaderboard Sort select trigger
    leaderboardSortSelect?.addEventListener('change', (e) => {
      renderLeaderboard((e.target as HTMLSelectElement).value);
    });

    // 8. Listen to hash routing changes
    window.addEventListener('hashchange', handleRouting);
    // Listen to load routing checks
    handleRouting();
  };

  // Run on page load
  document.addEventListener('astro:page-load', init);
  // Fallback for direct load (in static builds without transition)
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
</script>
