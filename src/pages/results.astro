---
import Layout from '../layouts/Layout.astro';
import resultsData from '../../public/data/results.json';
import { GraduationCap, Trophy, Award, TrendingUp, CheckCircle, HelpCircle, ArrowUpRight, ShieldCheck, Sparkles, Calculator } from '@lucide/astro';

// Results data destructured
const { announcements, passPercentageBySemester, passPercentageByDepartment, yearlyTrends } = resultsData;
---

<Layout title="Results Portal" activeTab="results">

  <!-- Header -->
  <div style="margin-bottom: 2rem; display: flex; flex-direction: column; gap: 0.5rem;">
    <h1 style="font-size: 2rem; font-family: var(--font-display); tracking: -0.03em;">Results & Analytics Portal</h1>
    <p style="color: var(--text-secondary); max-width: 600px; font-size: 1.05rem;">
      Check examination result updates, department toppers, passing statistics, and estimate your CGPA.
    </p>
  </div>

  <!-- Main Grid Layout -->
  <div style="display: grid; grid-template-columns: 1fr; gap: 1.5rem; margin-bottom: 2.5rem;" class="md:grid-cols-3">
    
    <!-- Left 2 Cols: Main Results Info -->
    <div class="md:col-span-2" style="display: flex; flex-direction: column; gap: 2rem;">
      
      <!-- Results Announcements -->
      <div>
        <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <CheckCircle style="color: var(--accent-success);" /> Result Declarations
        </h2>
        
        <div style="display: flex; flex-direction: column; gap: 1rem;">
          {announcements.map(res => {
            const isDeclared = res.status === 'Declared';
            return (
              <div class="card" style={`display: flex; align-items: center; justify-content: space-between; gap: 1rem; border-left: 4px solid ${isDeclared ? 'var(--accent-success)' : 'var(--accent-warning)'};`}>
                <div>
                  <h3 style="font-size: 1rem; font-weight: 700; color: var(--text-primary);">{res.title}</h3>
                  <p style="font-size: 0.8rem; color: var(--text-secondary); margin-top: 0.15rem;">
                    Status: <span style={`font-weight: 600; color: ${isDeclared ? 'var(--accent-success)' : 'var(--accent-warning)'};`}>{res.status}</span>
                    {isDeclared && ` • Declared on ${res.date}`}
                  </p>
                </div>
                
                {isDeclared ? (
                  <a href={res.link} target="_blank" rel="noopener noreferrer" class="btn btn-outline btn-sm" style="font-size: 0.8rem; gap: 0.25rem; white-space: nowrap;">
                    View Marks <ArrowUpRight size="14" />
                  </a>
                ) : (
                  <span class="badge badge-warning" style="font-size: 0.75rem;">Awaiting</span>
                )}
              </div>
            );
          })}
        </div>
      </div>

      <!-- CSS-Based Visualizations: Pass Rates by Semester -->
      <div>
        <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <TrendingUp style="color: var(--accent-primary);" /> Semester Pass Rates
        </h2>
        
        <div class="card" style="display: flex; flex-direction: column; gap: 1.25rem;">
          <p style="font-size: 0.875rem; color: var(--text-secondary); margin-bottom: 0.5rem;">
            Comparative analysis of pass percentages across the undergraduate batches.
          </p>
          
          {passPercentageBySemester.map(item => (
            <div style="display: flex; flex-direction: column; gap: 0.35rem;">
              <div style="display: flex; justify-content: space-between; align-items: center; font-size: 0.85rem;">
                <span style="font-weight: 700; color: var(--text-primary);">{item.semester}</span>
                <span style="font-size: 0.8rem; color: var(--text-secondary);">
                  <strong style="color: var(--text-primary);">{item.passPercentage}%</strong> ({item.totalStudents} students)
                </span>
              </div>
              
              <!-- Custom styled progress bar -->
              <div style="background-color: var(--bg-tertiary); height: 10px; border-radius: var(--radius-full); overflow: hidden; display: flex;">
                <div 
                  style={`width: ${item.passPercentage}%; background: linear-gradient(90deg, var(--accent-primary) 0%, var(--accent-success) 100%); border-radius: var(--radius-full);`}
                ></div>
              </div>
            </div>
          ))}
        </div>
      </div>

      <!-- Department Toppers -->
      <div>
        <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <Trophy style="color: var(--accent-warning);" /> Department Toppers (NEP Batch)
        </h2>
        
        <div style="display: grid; grid-template-columns: 1fr; gap: 1rem;" class="md:grid-cols-2">
          {passPercentageByDepartment.map(dept => (
            <div class="card" style="display: flex; flex-direction: column; gap: 0.75rem;">
              <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px solid var(--border-color); padding-bottom: 0.5rem;">
                <h3 style="font-size: 1.05rem; font-weight: 700;">{dept.department}</h3>
                <span class="badge badge-success" style="font-size: 0.75rem;">{dept.passPercentage}% Pass</span>
              </div>
              
              <div style="display: flex; flex-direction: column; gap: 0.5rem;">
                {dept.toppers.map((topper, idx) => (
                  <div style="display: flex; align-items: center; gap: 0.5rem; font-size: 0.85rem;">
                    <Award size="16" style={`color: ${idx === 0 ? 'var(--accent-warning)' : 'var(--text-tertiary)'};`} />
                    <span style="color: var(--text-primary); font-weight: 500;">{topper}</span>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>

    </div>

    <!-- Right 1 Col: Dynamic CGPA Calculator & Reminders -->
    <div style="display: flex; flex-direction: column; gap: 2rem;">
      
      <!-- Interactive SGPA to CGPA Calculator -->
      <div class="card" style="border-color: var(--accent-primary);">
        <h2 style="font-size: 1.25rem; font-family: var(--font-display); margin-bottom: 1rem; display: flex; align-items: center; gap: 0.5rem;">
          <Calculator style="color: var(--accent-primary);" /> CGPA Calculator
        </h2>
        
        <p style="font-size: 0.8rem; color: var(--text-secondary); margin-bottom: 1.25rem;">
          Select Semesters and input your SGPAs to compute your cumulative CGPA and percentage according to Kolhan University CBCS rules.
        </p>

        <!-- Calculator Form -->
        <div style="display: flex; flex-direction: column; gap: 0.75rem;" id="cgpa-calc-inputs">
          {[1, 2, 3, 4, 5, 6, 7, 8].map(sem => (
            <div style="display: flex; align-items: center; gap: 0.75rem; justify-content: space-between;">
              <label style="display: flex; align-items: center; gap: 0.5rem; font-size: 0.85rem; font-weight: 600; cursor: pointer;">
                <input type="checkbox" class="sem-check" data-sem={sem} checked={sem <= 2} style="cursor: pointer; width: 16px; height: 16px; accent-color: var(--accent-primary);" />
                <span>Semester {sem}</span>
              </label>
              
              <input 
                type="number" 
                class="sem-sgpa form-input" 
                data-sem={sem} 
                min="0" 
                max="10" 
                step="0.01" 
                placeholder="SGPA (0-10)" 
                style={`width: 100px; padding: 0.35rem 0.5rem; font-size: 0.85rem; text-align: right; height: 32px; display: ${sem <= 2 ? 'block' : 'none'};`} 
              />
            </div>
          ))}
        </div>

        <!-- Result Box -->
        <div style="background-color: var(--bg-tertiary); border: 1px dashed var(--border-color); border-radius: var(--radius-sm); padding: 1rem; margin-top: 1.25rem; display: flex; flex-direction: column; gap: 0.5rem; text-align: center;">
          <div style="font-size: 0.75rem; font-weight: 700; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.05em;">Estimated CGPA</div>
          <div id="cgpa-output" style="font-size: 2rem; font-family: var(--font-display); font-weight: 800; color: var(--accent-primary); line-height: 1;">0.00</div>
          
          <div style="display: flex; justify-content: space-between; font-size: 0.8rem; border-top: 1px solid var(--border-color); padding-top: 0.5rem; margin-top: 0.25rem;">
            <span style="color: var(--text-secondary);">Percentage:</span>
            <span id="percent-output" style="font-weight: 700; color: var(--text-primary);">0.0%</span>
          </div>
          <div style="display: flex; justify-content: space-between; font-size: 0.8rem;">
            <span style="color: var(--text-secondary);">Classification:</span>
            <span id="class-output" style="font-weight: 700; color: var(--text-primary);">-</span>
          </div>
        </div>

        <p style="font-size: 0.7rem; color: var(--text-tertiary); margin-top: 0.75rem; text-align: center;">
          *Formula: Percentage = CGPA * 9.5. Minimum pass marks is 4.0 SGPA.
        </p>
      </div>

      <!-- Postponed Chart.js Reminder Widget -->
      <div class="card" style="background: linear-gradient(135deg, rgba(99, 102, 241, 0.05) 0%, rgba(16, 185, 129, 0.05) 100%); border: 1px dashed var(--border-color); padding: 1.25rem;">
        <h3 style="font-size: 0.95rem; font-weight: 700; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.4rem; color: var(--accent-primary);">
          <Sparkles size="16" /> Phase 2 Upgrades
        </h3>
        <p style="font-size: 0.8rem; color: var(--text-secondary); line-height: 1.4;">
          <strong>Interactive Analytics Reminder</strong>: Chart.js dynamic visual representations (Semester-wise bar graphs, yearly linear trends, and comparison templates) will be configured in the next build session.
        </p>
      </div>

    </div>

  </div>

  <script>
    // Real-time CGPA calculator implementation
    const calcInputsContainer = document.getElementById('cgpa-calc-inputs');
    const checkboxes = calcInputsContainer?.querySelectorAll('.sem-check') as NodeListOf<HTMLInputElement>;
    const sgpaInputs = calcInputsContainer?.querySelectorAll('.sem-sgpa') as NodeListOf<HTMLInputElement>;
    
    const cgpaOutput = document.getElementById('cgpa-output');
    const percentOutput = document.getElementById('percent-output');
    const classOutput = document.getElementById('class-output');

    const recalculateCGPA = () => {
      let sum = 0;
      let count = 0;

      sgpaInputs.forEach(input => {
        const sem = input.getAttribute('data-sem') || '';
        const matchingCheckbox = calcInputsContainer?.querySelector(`.sem-check[data-sem="${sem}"]`) as HTMLInputElement;
        
        if (matchingCheckbox?.checked) {
          const val = parseFloat(input.value) || 0;
          sum += val;
          count++;
        }
      });

      if (count === 0) {
        if (cgpaOutput) cgpaOutput.textContent = '0.00';
        if (percentOutput) percentOutput.textContent = '0.0%';
        if (classOutput) classOutput.textContent = '-';
        return;
      }

      const cgpa = sum / count;
      const percentage = cgpa * 9.5;

      let classification = 'Fail';
      if (cgpa >= 7.5) {
        classification = '1st Class with Distinction';
      } else if (cgpa >= 6.0) {
        classification = '1st Class';
      } else if (cgpa >= 5.0) {
        classification = '2nd Class';
      } else if (cgpa >= 4.0) {
        classification = 'Pass';
      }

      if (cgpaOutput) cgpaOutput.textContent = cgpa.toFixed(2);
      if (percentOutput) percentOutput.textContent = percentage.toFixed(1) + '%';
      if (classOutput) classOutput.textContent = classification;
    };

    // Listen to changes
    checkboxes?.forEach(box => {
      box.addEventListener('change', (e) => {
        const target = e.target as HTMLInputElement;
        const sem = target.getAttribute('data-sem') || '';
        const input = calcInputsContainer?.querySelector(`.sem-sgpa[data-sem="${sem}"]`) as HTMLInputElement;
        
        if (input) {
          input.style.display = target.checked ? 'block' : 'none';
          if (!target.checked) {
            input.value = '';
          }
        }
        recalculateCGPA();
      });
    });

    sgpaInputs?.forEach(input => {
      input.addEventListener('input', recalculateCGPA);
    });

    // Run once on load
    recalculateCGPA();
  </script>

</Layout>
